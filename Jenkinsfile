pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git credentialsId: 'github-pat', branch: 'dev', url: 'https://github.com/ramishshakeel1/Project-Mlops.git'
            }
        }

    }S
}